Factory Constructor

มี return ได้

ไม่ต้องสร้าง object ใหม่ทุกครั้ง

คืน object ที่มีอยู่แล้วก็ได้

คืน subclass ก็ได้

คืน null ก็ยังได้ (ถ้าเป็น nullable)

✅ Constructor ปกติ

ไม่มี return

สร้าง object ใหม่เสมอ

ทำได้เฉพาะการกำหนดค่าให้ตัวแปรใน class

✅ Factory Constructor

มี return ได้

ไม่ต้องสร้าง object ใหม่ทุกครั้ง

คืน object ที่มีอยู่แล้วก็ได้

คืน subclass ก็ได้

คืน null ก็ยังได้ (ถ้าเป็น nullable)

```dart

class A {
  factory A() {
    return A._internal(); // return object
  }

  A._internal();
}

```

🧠 ทำไม factory มีประโยชน์ใน Flutter?
✔ 1. ใช้ parse JSON model ได้ง่าย → network app ทุกตัวต้องใช้
✔ 2. ช่วยทำ Singleton เช่น Logger, Config
✔ 3. ควบคุมการสร้าง object → ลดการสร้างซ้ำ
✔ 4. ทำให้โค้ด clean และอ่านง่าย
✔ 5. ใช้สร้าง object จากหลายรูปแบบ input ได้

ความแตกต่างสำคัญที่สุด
✔ Dart factory = constructor ที่ “ควบคุมการ return object ได้”

เช่น return object เดิม

```dart
factory User.instance() {
  return _singleton;
}
```

📦 ตัวอย่างที่เหมือนกันแบบ 100%

```dart
Dart:
var user = User.fromJson({"name": "Tob", "age": 34});

TypeScript:
const user = User.fromJson({ name: "Tob", age: 34 });


โค้ดใช้งานเหมือนกัน แต่ วิธีสร้างต่างกัน:

Dart = factory constructor

TypeScript = static method
```

🎯 สรุปง่ายที่สุด

factory ใน Dart = static method ใน TypeScript
แต่ factory มีความสามารถมากกว่า เพราะ:

เลือกจะสร้างหรือไม่สร้าง object ได้

คืน object อื่นได้

ทำ singleton ได้

ทำ caching ได้

TypeScript ทำแบบนี้ได้ แต่ต้องใช้ static & patterns
ไม่ใช่ constructor โดยตรง

## ตัวอย่าง

```dart
ตัวอย่างที่เข้าใจง่ายที่สุด (ง่ายกว่า Singleton)
🧃 ตัวอย่าง 1: ร้านน้ำผลไม้

คุณไปสั่งน้ำ:

Juice drink = Juice.fromFruit("orange");


โรงงาน (factory) จะเลือกให้ว่า:

ถ้าบอก "orange" → เอาน้ำส้ม

ถ้าบอก "apple" → เอาน้ำแอปเปิ้ล

ถ้าบอกอย่างอื่น → เอาน้ำเปล่า

โค้ด:
class Juice {
  final String flavor;

  Juice(this.flavor);

  factory Juice.fromFruit(String fruit) {
    if (fruit == "orange") return Juice("Orange Juice");
    if (fruit == "apple") return Juice("Apple Juice");
    return Juice("Water");
  }
}


ใช้งาน:

var j = Juice.fromFruit("orange");
print(j.flavor); // Orange Juice


✔ นี่คือ factory: “เลือกสร้าง object ที่เหมาะที่สุด”

🍪 ตัวอย่าง 2: ไม่สร้างใหม่ทุกครั้ง (ประหยัดเหมือนแชร์แก้วน้ำ)
class Counter {
  static final Counter _instance = Counter._internal();

  factory Counter() {
    return _instance; // คืน object ตัวเดิมทุกครั้ง
  }

  Counter._internal();
}


ใช้:

print(Counter() == Counter()); // true


✔ Factory ทำให้ทุกครั้งที่เรียก → ได้ object ตัวเดิม

🍙 ตัวอย่าง 3: แปลง JSON → object

(ใช้งานบ่อยมากใน Flutter)

class User {
  final String name;
  final int age;

  User(this.name, this.age);

  factory User.fromJson(Map json) {
    return User(json["name"], json["age"]);
  }
}


ใช้:

var u = User.fromJson({"name": "Tob", "age": 34});

```
