import 'package:flutter/material.dart';

class AppColors {
  // common
  static const primary = Color(0xFF1565C0); // น้ำเงินเข้ม = ความมั่นคง
  static const elevatedButton = Color.fromARGB(255, 42, 94, 224);
  static const accentColor = Color(0xFFFFC107);

  //light mode
  static const lightModeBackground = Color(0xFFF2F2F2);
  static const textLightMode = Color.fromARGB(255, 49, 49, 49);

  // dark mode
  static const darkModeBackground = Color.fromARGB(255, 27, 26, 26);
  static const textDarkMode = Color.fromARGB(255, 255, 255, 255);

  static const secondary = Color(0xFFFFC107); // เหลืองทอง = โดดเด่น/เชื่อมั่น
  static const textPrimary = Color(0xFF212121); // ดำอ่อน = อ่านง่าย
  static const textSecondary = Color(0xFF757575); // เทาเข้ม = รอง
  static const success = Color(0xFF4CAF50); // เขียว = ความสำเร็จ
}
