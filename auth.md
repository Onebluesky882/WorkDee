ใช้ firebase_auth + local_auth ช่วยให้ UX ดีขึ้น

เช่น user login ด้วย Firebase (email/password) ครั้งแรก

หลังจากนั้นใช้ FaceID / Fingerprint (local_auth) เพื่อ unlock app

keep section lcoal storeage
Hive

Authenticate user locally
local_auth
